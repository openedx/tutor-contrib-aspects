
{% if ASPECTS_ENABLE_STUDIO_IN_CONTEXT_METRICS %}

    const { AspectsSidebar } = await import("@openedx/frontend-plugin-aspects/src/components/AspectsSidebar");
    const { AspectsSidebarProvider } = await import("@openedx/frontend-plugin-aspects/src/components/AspectsSidebarContext");
    const { CourseHeaderButton } = await import("@openedx/frontend-plugin-aspects/src/components/CourseHeaderButton");
    const { SidebarToggleWrapper } = await import("@openedx/frontend-plugin-aspects/src/components/SidebarToggleWrapper");
    const { UnitActionsButton } = await import("@openedx/frontend-plugin-aspects/src/components/UnitActionsButton");

{% else %}
      // Currently there is no way to conditionally add plugin slot configuration from tutor-mfe.
      // So, as a workaround, tutor-contrib-aspects simply defines the components added to the plugin as empty components.
      const OutlineAnalytics = () => (<></>);
      const UnitAnalytics = () => (<></>);
{% endif %}
