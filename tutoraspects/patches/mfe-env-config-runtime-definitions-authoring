
{% if ASPECTS_ENABLE_STUDIO_IN_CONTEXT_METRICS %}

    const { default: AspectsSidebar } = await import("@openedx/frontend-plugin-aspects/components/AspectsSidebar");
    const { default: AspectsSidebarProvider } = await import("@openedx/frontend-plugin-aspects/components/AspectsSidebarContext");
    const { default: CourseHeaderButton } = await import("@openedx/frontend-plugin-aspects/components/CourseHeaderButton");
    const { default: SidebarToggleWrapper } = await import("@openedx/frontend-plugin-aspects/components/SidebarToggleWrapper");
    const { default: UnitActionsButton } = await import("@openedx/frontend-plugin-aspects/components/UnitActionsButton");

{% else %}
      // Currently there is no way to conditionally add plugin slot configuration from tutor-mfe.
      // So, as a workaround, tutor-contrib-aspects simply defines the components added to the plugin as empty components.
      const OutlineAnalytics = () => (<></>);
      const UnitAnalytics = () => (<></>);
{% endif %}
