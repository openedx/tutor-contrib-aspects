# Configuration needed for openedx-event-sink-clickhouse
EVENT_SINK_CLICKHOUSE_BACKEND_CONFIG = {
        "url": "http://clickhouse:8123",
        "username": "{{ OARS_CLICKHOUSE_CMS_USER }}",
        "password": "{{ OARS_CLICKHOUSE_CMS_PASSWORD }}",
        "database": "{{ OARS_EVENT_SINK_DATABASE }}",
        "timeout_secs": 3
}
